<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ job_title }} - Job Published</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                color: #333;
            }

            h1 {
                color: #4CAF50;
            }

            .footer {
                font-size: 12px;
                color: #aaa;
            }
        </style>
    </head>

    <body>
        <h1>{{ "New Job Listing" if not is_company else "Listing Published" }}: {{ job_title }}</h1>
        <p>Hello {{ recipient_name }},</p>

        {% if is_company %}
        <p>Your job listing has been successfully published and is now live on the job board.</p>
        {% else %}
        <p>We are excited to inform you about a new job posting:</p>
        {% endif %}

        <ul>
            <li><strong>Title:</strong> {{ job_title }}</li>
            <li><strong>Company:</strong> {{ company_name }}</li>
            <li><strong>Location:</strong> {{ job_location }}</li>
        </ul>

        {% if not is_company %}
        <p><a href="{{ job_url }}" style="color: #4CAF50;">Click here</a> to view the job listing and apply.</p>
        {% endif %}

        <p>Best regards,<br>Your Job Board Team</p>
        <hr>
        <p class="footer">
            {% if is_company %}
            You are receiving this message because you posted a job listing.
            {% else %}
            This email was sent to you because you are subscribed to job notifications.
            {% endif %}
        </p>
    </body>

</html>